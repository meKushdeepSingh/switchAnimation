/**
 * @format
 */

import { AppRegistry } from 'react-native';
import { name as appName } from './app.json';
import { SwitchScreen } from './feature/SwitchScreen';

AppRegistry.registerComponent(appName, () => SwitchScreen);
